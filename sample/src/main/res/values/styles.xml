<resources>

    <!-- Only used as the pre-Compose window theme (status bar, splash background, etc);
         actual UI theming is done in Compose via SampleTheme.kt. -->
    <style name="AppTheme" parent="Theme.Material3.DayNight.NoActionBar" />

</resources>
